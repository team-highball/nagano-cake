<h3>注文履歴詳細</h3>

<table>
    <thead>
        <tr>
            <th>購入者</th>
            <th><%= link_to @client.last_name + @client.first_name ,admin_client_path(@client) %></th>
        </tr>
    </thead>

    <tbody>
        <tr>
            <td>配送先</td>
            <td><%= "〒"+@order.postal_code +  @order.address + @order.name %></td>
        </tr>

        <tr>
            <td></td>
            <td></td>
        </tr>

        <tr>
            <td>支払方法</td>
            <td>
                <% if @order.payment_method == 1%>
                    クレジットカード
                <% else %>
                    銀行振込
                <% end %>            
            </td>
        </tr>

        <tr>
            <td>注文ステータス</td>
            <td>
                <% if @order.status == 1%>
                <% elsif @orders.status == 2 %>
            </td>
        </tr>

    </tbody>
</table>