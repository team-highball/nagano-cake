<table class="table table-hover">
  <thead class="thead-dark">
    <tr>
      <th>ジャンル検索</th>
    </tr>
  </thead>
  <tbody class="overflow-auto">
    <% @genres.each do |genre| %>
    <tr>
      <td>
        <%= link_to genre.name, client_products_path(genre_sort: genre.id) %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
